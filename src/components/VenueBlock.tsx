import VenueCarousel from "./VenueCarousel";

const VenueBlock = () => (
  <section className="bg-pastel2 pt-4 pb-12 px-4 text-center">
    <VenueCarousel />
  </section>
);

export default VenueBlock;
